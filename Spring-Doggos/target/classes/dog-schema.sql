DROP TABLE IF EXISTS `doggo` CASCADE;

CREATE TABLE `doggo` (
	`id` INTEGER PRIMARY KEY AUTO_INCREMENT,
	`diet` VARCHAR(255) NOT NULL,
	`age` INTEGER NOT NULL,
	`breed` VARCHAR(255) NOT NULL
);